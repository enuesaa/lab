schema {
  query: Query
  mutation: Mutation
}

type Query {
  notes(channel: String!): [Note!]!
}

type Mutation {
  createNote(message: String!): Note!
}

type Note {
  id: ID!
  message: String!
  channel: String!
}
