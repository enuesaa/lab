AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
# Description: Sample App

Resources:
  MemosTable:
    Type: AWS::DynamoDB::Table
    Properties:
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: id
          AttributeType: S
      KeySchema:
        - AttributeName: id
          KeyType: HASH

  MemosFunction:
    Type: AWS::Serverless::Function
    Properties:
      PackageType: Image
      Timeout: 30
      MemorySize: 512
      Architectures: [arm64]
      Environment:
        Variables:
          APP_ENV: prod
          APP_DEBUG: "0"
          APP_SECRET: SampleAppSecret
          DYNAMODB_TABLE: !Ref MemosTable
      FunctionUrlConfig:
        AuthType: NONE
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref MemosTable
    Metadata:
      Dockerfile: Dockerfile
      DockerContext: .
      DockerTag: lambda
      DockerBuildTarget: lambda

Outputs:
  FunctionUrl:
    Value: !GetAtt MemosFunctionUrl.FunctionUrl
  TableName:
    Value: !Ref MemosTable
